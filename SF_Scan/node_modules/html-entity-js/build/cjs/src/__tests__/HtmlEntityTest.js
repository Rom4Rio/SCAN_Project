"use strict";

var _chai = require("chai");

var _index = require("../index");

describe('Test Html Encode', function () {
  it('basic test', function () {
    var acture = (0, _index.htmlEncode)('&');
    (0, _chai.expect)(acture).to.equal('&amp;');
  });
});
describe('Test Html Encode', function () {
  it('basic test', function () {
    var acture = (0, _index.htmlDecode)('&amp;');
    (0, _chai.expect)(acture).to.equal('&');
  });
});